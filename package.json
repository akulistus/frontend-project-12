{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "A simplified slack chat clone",
  "main": "src/index.js",
  "workspaces": [
    "frontend"
  ],
  "proxy": "http://localhost:5001",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --prefix frontend",
    "start": "npx start-server -s ./frontend/build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.4"
  }
}
